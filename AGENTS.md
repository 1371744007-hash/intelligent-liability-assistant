# 项目协作约定

1. 修改代码前必须阅读 `docs/REQUIREMENTS.md` 和 `docs/RULES.md`。
2. 不得擅自修改模拟业务规则；规则变更必须由项目所有者明确提出。
3. 完成修改后必须更新 `STATUS.md`，如实记录完成进度、测试结果、失败项和未完成事项。
4. 程序功能测试与真实模型评测分开记录；未调用 API 的测试不得计入判责准确率。
5. API 密钥仅放在服务端 `.env` 环境变量中，禁止写入前端、测试数据或 Git。

