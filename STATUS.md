# 当前状态

更新时间：2026-09-11

## 已完成

- [x] 项目说明、需求、规则和协作约定
- [x] 8 个模拟订单、覆盖 R001–R008 的案例及 7 个边界案例
- [x] 中文工作台、订单回填、案例填入、多图预览/删除/分类、规则查看
- [x] 服务端 Responses API 文字与图片请求；密钥仅从 `.env` 读取
- [x] 严格 JSON Schema 与责任/赔付/追偿一致性校验
- [x] 未配置、API 异常、超时、图片读取失败的明确提示
- [x] 功能自动化测试
- [x] 本地服务启动并提供 `http://localhost:3000` 预览
- [x] 报案信息整合为单一模块，验货项目改为清单展示
- [x] 新增自定义模拟报案（商品、验货服务、验货项目、验货结果）
- [x] 边界案例继续保留用于测试，但不在页面演示下拉框展示
- [x] 将底层 `fetch failed` 转换为可操作的中文网络、DNS 或超时提示
- [x] 新增阿里云百炼 OpenAI 兼容接口，默认使用 `qwen-vl-max` 处理文字和图片
- [x] 增加 Node.js 环境代理启动器，并按本机 FlClash 端口 `127.0.0.1:7890` 配置
- [x] 已创建本地 `.env` 配置文件模板（密钥留空且被 Git 忽略）
- [x] 项目名称已统一调整为“智能判责助手”，并移除原名称
- [x] 已发布到 GitHub 公开仓库：`https://github.com/1371744007-hash/intelligent-liability-assistant`
- [x] 新增 Render 免费部署配置，百炼密钥仅作为服务端秘密环境变量录入
- [x] 新增 Vercel Functions 适配，并按其请求限制约束上传图片体积

## 测试记录

- Vercel 适配后完整测试：通过（9/9）；新增覆盖 Vercel 启动数据与无效订单拦截。
- 项目改名后回归测试：通过（7/7）；全文检查未发现原项目名称残留。
- 程序功能测试：通过（7/7）。覆盖结构化结果校验、业务冲突转人工、非法字段拒绝、静态页面访问、页面仅返回 8 个主案例、预期答案隔离和无效订单拦截。
- 真实模型评测：共尝试 3 次。前两次 OpenAI 分别因网络超时、无额度而未获得结论；第 3 次使用阿里云百炼 `qwen-vl-max` 成功完成 C001 纯文本测试。
- 百炼 C001 实测：核心预期字段通过（商家责 / 不赔付 / 不追偿 / R001），图片观察为空；模型另因系统记录与报案描述冲突建议人工核实。当前真实评测核心字段通过 1/1，但样本量不足，不宣称整体判责准确率。

## 待办

- API 密钥及本地代理已配置；待 API 账户充值后运行真实模型评测并如实记录结果。
- 当前已切换并成功接通阿里云百炼；后续可继续运行其余主案例与边界案例评测。
- 如需图片维度评测，由项目所有者提供合法的模拟图片样本；不生成虚假的图片观察记录。
- GitHub 当前提供源码展示，尚未部署为公网可访问的在线 Demo；本地预览仍使用 `http://localhost:3000`。
- 待在 Render 控制台连接 GitHub、填写百炼密钥并完成首次部署验证。
- Render 要求当前账号绑卡验证，改用 Vercel 免费部署；待在 Vercel 导入仓库并配置百炼密钥。
